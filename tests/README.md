# Repository-Level Tests

Cross-module integration, production-snapshot upgrade, browser, performance, and release smoke tests belong here. Tests owned by one module should normally remain inside that module's `tests/` directory.
