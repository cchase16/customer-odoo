/** @odoo-module **/

/**
 * PH-01 asset-load probe.
 *
 * The implementation is intentionally inert until the approved framework
 * phases add the registry, adapter, coordinator, and providers.
 */
export const CUSTOMER_FORM_CONTEXT_MENU_SCAFFOLD = true;
