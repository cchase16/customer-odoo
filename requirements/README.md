# Dependencies

Pinned build-time and runtime dependency definitions belong here. Factory dependency resolution in `factory.lock` does not replace the product's Python or JavaScript dependency locks.
